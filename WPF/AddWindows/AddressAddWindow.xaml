<Window x:Class="WPF.AddressAddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Address" Height="200" Width="400">
    <Grid>
        <StackPanel Margin="10">
            <!-- Street Address -->
            <TextBlock Text="Street Address:" HorizontalAlignment="Center"/>
            <TextBox x:Name="StreetAddress" Width="200"/>

            <!-- ZIP -->
            <TextBlock Text="ZIP Code:" HorizontalAlignment="Center"/>
            <TextBox x:Name="PostalCode" Width="200"/>

            <!-- City -->
            <TextBlock Text="City:" HorizontalAlignment="Center"/>
            <TextBox x:Name="City" Width="200"/>

            <!-- Bouton de sauvegarde -->
            <Button Content="Save" Width="100" Margin="0,20,0,0" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
